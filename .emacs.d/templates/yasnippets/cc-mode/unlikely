# -*- mode: snippet; require-final-newline: nil -*-
# name: unlikely
# key: ul
# --
${1:_}${1:$(if (string-match "^_" yas-text)
               "unlikely"
               (if (string-match "^u" yas-text)
                   "nlikely" ""))}($0)